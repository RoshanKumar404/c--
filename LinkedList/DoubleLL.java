public abstract class DoubleLL {
    // In this only the node.prev i sneing added
     /*
      for eg node.next= head;
      head.prev= node;
      node.prev=null;
      node= head;
      just it
     */
}
